class TransmissionControlUnit 

signature: 

	visible: controlCommandDetach, controlCommandAttach, controlGearUp, controlGearDown, controlGearReverse, sendSpeed;

	temporal domain: real;
	
	domains:
		Width: 1..2;

	items: 
		event controlCommandDetach;
		event controlCommandAttach;
		event controlGearUp (Width);
		event controlGearDown (Width);
		event controlGearReverse;
		TD sendSpeed (integer);

end